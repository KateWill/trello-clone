<div class="form-wrapper">
<h1><%= @board.id ? "Edit Board" : "Create New Board" %> </h1>

  <%= form_for @board do |b| %> 
    <%= b.label :name %>
    <%= b.text_field :name %>
    <%= b.submit %>
  <% end %>
</div>